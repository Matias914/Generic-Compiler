## --------------------------------------------------- ##
##  Prueba de casos validos del analizador sintactico  ##
## --------------------------------------------------- ##

## Sentencias Declarativas:
    Se muestra una sentencia declarativa válida tanto para
    una función como. Luego, se muestran algunos errores que
    fueron contemplados.
##

PROGRAM {
    uint X;
    uint Y, PROGRAMA.Z, W;
    uint T;

    uint FUNCTION(cr uint A, uint B, uint C) {
        uint ANIDADA(uint D) {
            return (trunc(-2.1));
        };
        A := B;
        ANIDADA(A+C->D);
        return (A);
    };
}

## Errores Sintácticos:
    Dentro de la lista de errores se hallan:
    - Declaración sin Variable
    - Declaración de Variable sin ":"
    - Declaración de Variables sin ","
    - Declaración de Variables sin Variables
    - Declaración de Función sin Nombre
    - Declaración de Función sin ";"
    - Parámetros Formales sin tipo
    - Parámetros Formales sin nombre
    - Función sin llave de cierre
    - Return con paréntesis faltantes
    - ...

    Se esperan errores en las líneas: 44, 45, 47, 47, 48, 48, 48, 49, 49, 49, 49, 50, 51, 51, 51
##

PROGRAMA {
    uint ;
    uint 1UI;
    uint X
    uint Y Z;
    uint Y , , , ;
    uint (uint A, B, cr, cr B) {
        return trunc(3UI));
